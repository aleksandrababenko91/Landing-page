import styles from './ArticlesSection.module.scss';
import items from './items';

export default function ArticlesSection() {
  return(
    <section className={styles.container}>
      <div className={styles.containerRow}></div>
    </section>
  )
}